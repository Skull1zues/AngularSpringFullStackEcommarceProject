import { CloneCartValidator } from './clone-cart-validator';

describe('CloneCartValidator', () => {
  it('should create an instance', () => {
    expect(new CloneCartValidator()).toBeTruthy();
  });
});
